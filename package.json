{
  "name": "velo-monorepo",
  "private": true,
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "cd packages/components && yarn test --ci ; cd ../app && yarn test --ci"
  }
}
